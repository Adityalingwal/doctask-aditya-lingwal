# sample-piles

Synthetic document piles the system runs on. Every file here is fabricated —
no real customer, project, or company appears in any of them.

One folder per pile. A pile is one project's documents, which is what a single
run consumes.

| Pile | Purpose |
|---|---|
| _(to be added)_ | The demo pile — what the walkthrough and the video use |
| _(to be added)_ | The second-run pile — different documents inside the same declared set, used to prove the system is not built around the demo |

Accepted formats and document types are declared in `DECISIONS.md`.
